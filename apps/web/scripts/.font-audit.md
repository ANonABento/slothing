# Font lint audit — 1 violation

Generated by `apps/web/scripts/forbidden-font-lint.cjs` against `apps/web/src/**`.

The lint flags three categories:

1. Tailwind defaults `font-sans` / `font-serif` (escape hatches that bypass the editorial token stack).
2. Inline `fontFamily:` in JSX/TSX with literal values. Allowed: `var(--…)`, `inherit`, `initial`, `unset`, empty string.
3. Raw `font-family:` declarations in CSS files outside the allowlist (`src/app/globals.css`, `src/lib/theme/**`, `src/styles/**`).

## By file (top 10)

1. `src/lib/og/template.tsx` — 1 violation

## Full listing

### src/lib/og/template.tsx

- :41 `fontFamily: "Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif"` (inline)

## Notes

- Zero `font-sans` / `font-serif` Tailwind utilities in the codebase. The editorial migration has already eliminated default Tailwind family classes.
- Zero raw `font-family:` declarations in non-allowlisted CSS files. Only `src/app/globals.css` exists today and contains no raw declarations either — the editorial token stack lives entirely in `tailwind.config.ts` + theme presets.
- The single remaining violation lives in the OG image template (`src/lib/og/template.tsx`). Satori (next/og's renderer) cannot resolve CSS variables or external stylesheets, so this site genuinely needs a literal stack. Either:
  - Refactor the OG template to receive the resolved family stack as a prop derived from the active theme preset, or
  - Add `src/lib/og/**` to an `EXEMPT_PATH_PATTERNS` list (same precedent as the existing color lint, which already exempts `src/lib/og/` and `opengraph-image.tsx` / `twitter-image.tsx` files because Satori has the same render constraints).

The current script intentionally does **not** carve out an OG exemption — the choice is deferred to a follow-up so this audit reflects ground truth.
