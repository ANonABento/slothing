# [DEAD] Delete stale TODO.md (refers to Clerk + Neon Postgres — wrong stack)

## Problem

`/home/anonabento/slothing/TODO.md` is a setup checklist for the **old Columbus codebase**, not the current Slothing stack. It instructs a developer to:

```
## 1. Clerk Authentication (Required)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

## 2. Neon PostgreSQL Database (Required)
DATABASE_URL=postgresql://username:password@ep-xxx.us-east-2.aws.neon.tech/columbus?sslmode=require
```

But the live stack is:
- **Auth:** NextAuth v5 + Google (`apps/web/src/auth.config.ts`, env vars `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXTAUTH_SECRET`).
- **DB:** libSQL/Turso (`apps/web/src/lib/db/index.ts` → `@libsql/client`, env `TURSO_DATABASE_URL`).

No `Clerk`, no `Neon`, no `DATABASE_URL=postgresql://...`:

```bash
$ grep -rEn 'Clerk|Neon|CLERK_SECRET_KEY' apps/web/src 2>/dev/null
# (empty)
$ grep -rEn 'CLERK_SECRET_KEY' apps/web/.env.example
# (no match — .env.example lists GOOGLE_CLIENT_* + TURSO_*)
```

## Why this matters

Any contributor (or AI agent in this very pipeline) reading `TODO.md` first is led directly into a wrong-stack rabbit hole. The README.md and `.env.example` are correct; TODO.md is the outlier.

## Fix

Option A (recommended): `git rm TODO.md` — the README + `.env.example` already cover production setup.

Option B: Rewrite TODO.md to reflect the real stack (NextAuth + libSQL + Resend + optional LLM providers from `apps/web/src/lib/env.ts:FEATURE_CHECKS`).

Prefer A unless someone is actively using TODO.md as a runbook (`grep -rn 'TODO.md' apps/web docs` returns one mention in apps/web — verify it is descriptive, not load-bearing).

## Acceptance criteria

- [ ] Either `TODO.md` is deleted, or it accurately reflects NextAuth + libSQL/Turso + Resend.
- [ ] No remaining mention of `Clerk`, `CLERK_SECRET_KEY`, `Neon`, or `postgresql://` in the repo outside `pnpm-lock.yaml` and historical commit messages.
- [ ] If kept: link to it from `README.md`. If deleted: confirm README covers the same checklist items.

## Files in scope

- `/home/anonabento/slothing/TODO.md`
- `/home/anonabento/slothing/README.md` (cross-check)

## Repo path

`/home/anonabento/slothing`


## Context
- Workspace: slothing
- Branch: bentoya/dead-delete-stale-todo-md-refers-to-clerk-neon-pos
- Working dir: /home/anonabento/slothing/.worktrees/bentoya-a3bb3715-ce8f-40d1-828f-1fc2a7f8ff5d
