Verify failed after one fix round.

Command run:
- `pnpm test:run`

Initial issue fixed:
- `apps/web/src/app/[locale]/(app)/studio/page.test.tsx` hung/fell over because `useStudioPageState`'s preview effect depended on `activeDocument.content` and `activeDocument.html` while also mutating those fields. I removed those self-triggering dependencies and verified the focused studio spec now passes.

Remaining failure:
- `apps/web/src/app/[locale]/(marketing)/components/footer.test.tsx`
- 2 failed tests:
  - `Footer > prefixes internal links with es and leaves section anchors unchanged`
  - `Footer > prefixes internal links with zh-CN and leaves section anchors unchanged`

What is still broken:
- The rendered footer hrefs include a localized pricing link (`/es/pricing`, `/zh-CN/pricing`) but the test expectation does not include that link.
- Full suite summary after the fix: 1 failed file, 389 passed; 2 failed tests, 2950 passed, 1 skipped.
